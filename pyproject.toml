[project]
name = "bioscfg"
version = "0.1.0"
description = "Read-only IFR/UEFI BIOS settings viewer"
requires-python = ">=3.11"

[project.scripts]
bioscfg-view = "bioscfg.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
